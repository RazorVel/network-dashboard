import React from "react";
import ReactDOM from "react-dom";
import FieldLookups from "./components/FieldLookups.js";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
    <React.StrictMode>
        <FieldLookups />
    </React.StrictMode>
);